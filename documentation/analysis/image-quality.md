# Image quality

DeepSight records blur, brightness, contrast, dimensions, and aspect ratio as advisory metadata. Warnings explain weak input but do not manufacture a model result. With several faces, the largest detected face is selected and its index, box, and detector confidence are recorded. A missing face returns `NO_FACE_DETECTED`.
