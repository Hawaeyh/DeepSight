# Stripe test mode

Stripe is disabled by default. With test keys and configured price IDs, authenticated users can create monthly or annual Checkout sessions and customer-portal sessions. DeepSight never handles card details and never activates a plan from the success URL.
