# Administrator monitoring

Database-verified administrators can inspect system totals, users, usage, video queue/failures, global model usage, verified confusion counts, health, and deployment-model availability. Metrics without stored evaluation artifacts display unavailable; DeepSight does not invent ROC, calibration, or training curves.
