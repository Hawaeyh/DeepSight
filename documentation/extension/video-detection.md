# Extension video detection

Sign in, open a page with a visible HTML video, and choose **Scan Visible Video**. DeepSight samples one frame every 1.5 seconds, maintains seven recent results, and supports pause/resume and stop. The scan is user initiated and bounded to the selected tab; each accepted sample uses the protected analysis API.
