# Extension image detection

Image scans are user initiated or explicitly enabled continuous scans. The content script filters tiny and duplicate images, limits concurrency, and clears overlays on navigation. The service worker attaches the stored JWT; the server derives ownership and never accepts client roles or owner identifiers.
